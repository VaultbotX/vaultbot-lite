{
  "name": "vaultbot-lite",
  "version": "0.1.0",
  "description": "Barebones implementation of the core functionality of Vaultbot",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --watch \"src/**\" --ext \"ts,json\" --ignore \"src/**/*.spec.ts\" --exec \"ts-node src/index.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1"
  }
}
